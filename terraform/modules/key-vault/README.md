# Azure Key Vault module

Key Vault base de Tidewatch, configurado con RBAC y soft delete. Los secretos
se añadirán mediante un flujo seguro de CI/CD o Workload Identity, nunca desde
archivos versionados.
